services:
  postgres:
    image: bitnami/postgresql:latest
    container_name: rocketlog
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: api-rocketlog
    ports:
      - "5432:5432"
